# Ondes Backend — Environment Variables Template
# Copier ce fichier en .env et remplir les valeurs

# Django Core
SECRET_KEY=change-me-to-a-random-50-char-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS — origines autorisées (séparées par virgule, vide = tout autoriser en dev)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Database
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3
# Pour PostgreSQL en production :
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=ondes_db
# DATABASE_USER=ondes_user
# DATABASE_PASSWORD=
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# Redis — Channel Layers (vide = InMemoryChannelLayer)
REDIS_URL=

# Rate limiting
RATE_LIMIT_ENABLED=False

# GENESIS — AI Mini-App creator (Anthropic Claude)
ANTHROPIC_API_KEY=sk-ant-...

# STRIPE — Monétisation GENESIS
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRO_MONTHLY_PRICE_ID=price_...
STRIPE_PRO_YEARLY_PRICE_ID=price_...
STRIPE_CREDIT_PACK_PRICE_ID=price_...
