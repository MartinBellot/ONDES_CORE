# ============================================================
#  Ondes Backend — Variables d'environnement PRODUCTION
#  Copier en .env.prod et remplir toutes les valeurs
#  ⚠️  Ne jamais committer .env.prod dans git
# ============================================================

# ── Django Core ──────────────────────────────────────────
SECRET_KEY=CHANGE_ME_TO_A_VERY_LONG_RANDOM_50_CHAR_STRING
DEBUG=False
ALLOWED_HOSTS=api.ondes.pro,72.62.28.63

# ── CORS ─────────────────────────────────────────────────
# Origines autorisées (app Flutter via Hostinger, site web...)
CORS_ALLOWED_ORIGINS=https://ondes.pro,https://www.ondes.pro

# ── Base de données PostgreSQL ────────────────────────────
# DATABASE_ENGINE et DATABASE_HOST sont injectés par docker-compose.prod.yml
DATABASE_NAME=ondes_db
DATABASE_USER=ondes_user
DATABASE_PASSWORD=CHANGE_ME_TO_A_STRONG_DB_PASSWORD

# ── Redis — Channel Layers ────────────────────────────────
# Injecté automatiquement par docker-compose.prod.yml
# REDIS_URL=redis://redis:6379

# ── GENESIS — Anthropic Claude ───────────────────────────
ANTHROPIC_API_KEY=sk-ant-XXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ── Stripe — Monétisation ─────────────────────────────────
STRIPE_SECRET_KEY=YOUR_STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=YOUR_STRIPE_WEBHOOK_SECRET
STRIPE_PRO_MONTHLY_PRICE_ID=YOUR_STRIPE_PRO_MONTHLY_PRICE_ID
STRIPE_PRO_YEARLY_PRICE_ID=YOUR_STRIPE_PRO_YEARLY_PRICE_ID
STRIPE_CREDIT_PACK_PRICE_ID=YOUR_STRIPE_CREDIT_PACK_PRICE_ID
